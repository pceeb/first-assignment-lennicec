Hello Students,

This is what you have to do for homework.
Remember the deadline to submit the HW is Wenesday at 8:00:00 am.

I. Read chapter 2 from the book Introduction to Scientific Computing for Biologists Author.   
The book is downloadable here: https://allesinalab.uchicago.edu/wp-content/uploads/2014/05/IntroSciComp2014.pdf

II. Answer the following question. What does this do?   

1. ls [A-Z]* - list all file beginnig with an uppercase letter 
2. cp *.sh /tmp - copy all files that end with ".sh" into trmporary files
3. man pwd - show help page of command pwd, will tell you what pwd does
4. cd ~ takes you to your home directory, same as cd $HOME
5. mkdir -p /test1/test2/test3 - create the directory test1, test2, and test3 
6. cp -r /tmp . - copy a directory, including all its subdirectories, from
 temporary files
7. ls | less - progressively print the first ten lines worth of the files on
 the screen 
8. head -n 24 test.txt » abc.txt - print the first few lines of using numeric 
values instead of dictionary for the file test.txt, and add the output from 
this command to a file called abc.txt
9. echo "aaa" > aaa.txt - redirect the printing of the string "aaa" to the
file called aaa.txt
10. ps -u sallesina - report information on current running processes 
under the user sallesina
11. top - program that displays running cpu/ memory utilization in an ordered
list 
12. ls | grep .sh | xargs mv /tmp - moves all the files that end in .sh into
 temproary files  
13. du -sk /home/* | sort -r -n | head -10 - display the first 10 lines of a 
numerical list of the files found in your home directory, including 
subdirectries, and sort them by estimated file space
14. sort -r names.txt - sort all the directories, including, the 
subdirectories that have a file called names.text 
